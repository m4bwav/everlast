Install the Everlast Protocol on this machine so nothing AI sessions learn is lost across tools. Do all of it, verify each step by its output, and report in five lines.

1. Clone the plugin: `git clone https://github.com/m4bwav/everlast` under the local plugin root (an existing marketplace folder such as ~/claude-plugins or D:\m4bwa\Claude\Projects\Ai if present; otherwise ~/claude-plugins). If it is already cloned, `git pull` instead. Private repository: run `gh auth login` first if git asks.
2. Clone your private vault (or create one with `everlast.py vault init`): `git clone <your vault repository>` to the path in the plugin's everlast.config.json for this OS (Windows: <vault>; otherwise ~/everlast-vault). If that drive does not exist, clone to ~/everlast-vault and set EVERLAST_VAULT to it. Already cloned: `git pull`.
3. Claude Code: `claude plugin marketplace add <plugin clone dir>` (if a marketplace already lists it, `claude plugin marketplace update <name>`), then `claude plugin install everlast-protocol@everlast --scope user` (or `@<marketplace name>`), then `claude plugin list` to confirm.
4. Other agents on this machine (Copilot, Codex, OpenCode, Windsurf): `python <plugin>/scripts/everlast.py export ~` so the skills appear under ~/.agents/skills. Cursor or Gemini CLI: also link them into ~/.cursor/skills or ~/.gemini/skills. Cowork: `python <plugin>/scripts/everlast.py pack` and upload the .plugin file through Customize > Plugins.
5. Verify: `python <plugin>/scripts/everlast.py vault status` and `project list`. Then read <vault>/user/PROFILE.md and the ENVIRONMENTS.md section for this machine (add a section if this machine is new, and log it with `everlast.py log . --user --op update --title "new machine <name>"`).

Report: plugin path and version, vault path and remote, projects registered, which tools got the skills, whether ENVIRONMENTS.md was updated.
ASK ME (once, before finishing): "Everlast can open draft pull requests on the official repository in your name containing the plugin's own learnings (its LEARNINGS, RESEARCH, CHANGELOG and TESTS files only; never your vault, your projects' docs or transcripts). Share learnings upstream? yes / no. Change it later with `everlast.py contribute yes|no`; nothing is sent until you answer, and no means nothing ever leaves this machine while updates still arrive with `git pull`." Then run `python (python3 on macOS and Linux) <plugin root>/scripts/everlast.py contribute <my answer>`; if I do not answer, run `contribute no`.
