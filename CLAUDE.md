@AGENTS.md

<!-- Claude-only additions below. Keep shared rules in AGENTS.md so other agents see them too. -->

# Claude notes

- This is the everlast plugin's own source. When installed, Claude Code runs a cached copy; edits here need `claude plugin marketplace update mark-local`, then uninstall and install of `everlast-protocol@mark-local`.
- The plugin and each skill are evergreen units in pointer mode; `evergreen-refresh`, `evergreen-learn`, `evergreen-test` and `evergreen-tune` apply.
