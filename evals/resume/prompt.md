---
description: Starting a task reads the index first and relies on the matching solution instead of re-exploring
tags: [smoke, resume]
max_turns: 15
allowed_tools: [Read, Glob, Grep, Skill]
---

Before you touch the build script, check whether we already ran into this dotnet SDK problem in this repo, and tell me what the fix was and whether it is still trustworthy.
